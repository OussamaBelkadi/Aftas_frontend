import {Time} from "@angular/common";

export class Competition {

  constructor( location: string, date: Date, startTime: Time, endTime: Time, amount: number) {
  }

  // code: string;
  // date: Date;
  // startTime: Time;
  // endTime: Time;
  // numberOfParticipants: number;
  // location: string;
  // amount: number;
  // status: string;

}
